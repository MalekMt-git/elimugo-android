/*
package ngo.dean.elimugo.repositories.models;

import java.util.Map;
import ngo.dean.elimugo.App;

public class ContentDescriptions {
    public Map<String, String> descriptions;

    public String getDescriptionByLocale(String locale) {
        if (descriptions.containsKey(locale)) {
            return descriptions.get(locale);
        }
        return descriptions.get(App.LOCALE);
    }

}
*/
